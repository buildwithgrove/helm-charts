apiVersion: v2
appVersion: "1.0"
description: A Helm chart for development and production deployments of GUARD (Gateway Utilities for Authentication, Routing & Defense)
name: guard
type: application
version: 0.1.20
dependencies:
  - name: gateway-helm
    repository: oci://docker.io/envoyproxy
    # TODO_TECHDEBT(@commoddity): Update to release version when v1.4.0 is released
    version: v1.4.0-rc.2
    condition: envoyGateway.enabled
